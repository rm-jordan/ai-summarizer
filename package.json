{
  "name": "ai-summarizer",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm run --prefix ./backend start\" \"npm run --prefix ./frontend dev\"",
    "dev:backend": "npm run --prefix ./backend start",
    "dev:frontend": "npm run --prefix ./frontend dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
